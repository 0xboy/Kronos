# Paper extras are now in pyproject.toml. Install the app:
#   .venv\Scripts\python.exe -m pip install -e .
#
# Kronos inference: prefer CUDA torch (GTX / RTX). Default pip torch is often CPU-only.
#   .venv\Scripts\python.exe -m pip install --force-reinstall torch --index-url https://download.pytorch.org/whl/cu128
# Verify:
#   .venv\Scripts\python.exe -c "import torch; print(torch.__version__, torch.cuda.is_available(), torch.cuda.get_device_name(0) if torch.cuda.is_available() else None)"
-e .
