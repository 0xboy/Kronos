# Prefer installing the app package (includes these deps):
#   .venv\Scripts\python.exe -m pip install -e .
#
# Prefer CUDA torch on NVIDIA machines (CPU wheel silently disables GPU):
#   .venv\Scripts\python.exe -m pip install --force-reinstall torch --index-url https://download.pytorch.org/whl/cu128
-e .
